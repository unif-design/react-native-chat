# Chat 示例

工作区名 `@unif/react-native-chat-example`，原生名 `ReactNativeChatExample`。使用 Community CLI 与 Vite Web 入口。

当前 App 验证 GestureHandlerRootView、SafeAreaProvider、Design ThemeProvider 和受控 Textarea 接线，不充当 Composer 或完整 Chat 实现。后续按组件契约添加独立示例，再组合主聊天与抽屉输入。

原生/Web 构建在 CI 执行，设备键盘、图片和手势分别验收。
