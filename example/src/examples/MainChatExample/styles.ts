import { StyleSheet } from 'react-native';
import { space } from '@unif/react-native-design';
export const styles = StyleSheet.create({
  root: { flex: 1, minHeight: 0 },
  chat: { padding: space[3] },
});
