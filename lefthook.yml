pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,jsx,tsx}'
      run: yarn exec eslint {staged_files}

    types:
      glob: '*.{js,ts,jsx,tsx}'
      run: yarn typecheck
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: yarn exec commitlint --edit
