import { StyleSheet } from 'react-native';
import { space } from '@unif/react-native-design';

export const styles = StyleSheet.create({
  root: { gap: space[4] },
  restore: { alignItems: 'flex-start' },
});
