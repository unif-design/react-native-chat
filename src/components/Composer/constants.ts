export const DEFAULT_MAX_INPUT_HEIGHT = 120;
export const PRIMARY_LABELS = {
  send: '发送',
  stop: '停止回复',
  busy: '正在处理…',
} as const;
export const VOICE_LABELS = {
  starting: '正在准备语音输入…',
  listening: '正在听取…',
  finishing: '正在完成识别…',
} as const;
